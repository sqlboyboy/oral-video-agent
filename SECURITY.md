# 安全与隐私

## 配置边界

- 本仓库不提供作者的云端账号、API 密钥、SSH 私钥或 Android 签名密钥。
- `api.example.com`、`gpu.example.com`、`user@example.com` 等内容均为占位示例。
- 实际配置保存在被 Git 忽略的 `.env`、部署秘密变量或系统安全存储中。
- 模板中的 `change-*`、`replace-*` 值不能用作正式部署凭据。
- 桌面本地 API 默认只监听回环地址；需要公网访问时，使用有身份校验的云端服务。

## 提交和报告

请勿在公开 Issue、PR、截图或日志中包含令牌、密码、Cookie、个人邮箱、私有部署信息和用户素材。发现问题时先提交不含敏感值的描述，以便维护者安排私下沟通。

仓库配置了 Gitleaks 历史扫描，并对凭据文件、运行数据及内部交接笔记设置忽略规则。扫描通过不代表对所有格式的秘密或图片内容都能完成检测。

## 如果凭据已经公开

应先在对应服务撤销或轮换凭据，再清理源码及提交历史。删除最新文件并不能自动移除历史副本；重写历史也无法撤回他人的克隆、分叉或缓存。

仓库历史清理后，已有克隆应重新克隆或按维护者说明同步，避免把旧历史再次推送。
